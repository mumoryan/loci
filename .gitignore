# OS
.DS_Store

# Logs and observability
logs/loci.db
logs/loci.db-shm
logs/loci.db-wal
logs/observability.db
logs/events.jsonl
logs/*.log
logs/raw-hook-payload.log
logs/session-*.log
logs/.session-state

# Agent files are expanded in-place by merge-agent.sh at session start (gitignored)
# Source state is preserved in git — merge-agent.sh overwrites for the session
# .claude/agents/*.md

# Local settings overrides
.claude/settings.local.json

# Worktrees (transient agent working directories)
worktrees/

# Runtime
node_modules/
.bun/
dist/
build/

# Environment
.env
.env.local

# Certs (generated locally for WebXR HTTPS dev)
certs/
.claude/scheduled_tasks.lock
