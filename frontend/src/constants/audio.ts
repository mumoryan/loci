export const AUDIO = {
  /** Volume level once the entry fade-in completes (0–1) */
  DEFAULT_VOLUME: 0.4,
  /** How long the volume ramps from 0 → DEFAULT_VOLUME after audio starts (ms) */
  FADE_IN_MS: 4500,
} as const
